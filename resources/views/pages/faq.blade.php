@extends('layouts.app')
@section('content')<section class="page-hero"><div class="container"><span class="eyebrow">Help centre</span><h1>Questions before the confetti.</h1><p>Booking, payment, city coverage and service information.</p></div></section><section class="section"><div class="container narrow-content">@foreach($faqs as $group=>$items)<div class="section-heading text-start mb-3"><span>{{ $group }}</span><h2>{{ \Illuminate\Support\Str::headline($group) }}</h2></div><div class="accordion mb-5" id="faqGroup{{ $loop->index }}">@foreach($items as $faq)<div class="accordion-item"><h3 class="accordion-header"><button class="accordion-button collapsed" data-bs-toggle="collapse" data-bs-target="#faqItem{{ $faq->id }}">{{ $faq->question }}</button></h3><div id="faqItem{{ $faq->id }}" class="accordion-collapse collapse" data-bs-parent="#faqGroup{{ $loop->parent->index }}"><div class="accordion-body">{{ $faq->answer }}</div></div></div>@endforeach</div>@endforeach</div></section>@endsection
