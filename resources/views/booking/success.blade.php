@extends('layouts.app')
@section('content')<section class="result-page success-result"><div class="result-card"><span class="result-icon"><i class="fa-solid fa-check"></i></span><span class="mini-label">Payment received</span><h1>Your party is reserved.</h1><p>Booking <strong>{{ $booking->booking_no }}</strong> is confirmed for {{ $booking->event_date->format('d M Y') }} in {{ $booking->city?->name }}.</p><div class="result-details"><div><span>Paid now</span><strong>&#8377;{{ number_format((float) $booking->latestPayment?->amount) }}</strong></div><div><span>Status</span><strong>{{ $booking->payment_status }}</strong></div></div><div class="d-flex gap-2 flex-wrap justify-content-center"><a class="btn btn-party" href="{{ route('dashboard.booking', $booking) }}">View booking</a><a class="btn btn-outline-party" href="{{ route('dashboard.booking.invoice', $booking) }}">Invoice</a></div></div></section>@endsection
