<article class="package-card h-100"><a class="package-media" href="{{ route('packages.show',$package) }}"><img src="{{ $package->image_url }}" alt="{{ $package->title }}" loading="lazy"><span>{{ $package->duration }}</span></a><div class="package-body"><span class="mini-label">{{ $package->trending ? 'Trending package' : 'Complete party plan' }}</span><h3><a href="{{ route('packages.show',$package) }}">{{ $package->title }}</a></h3><p>{{ \Illuminate\Support\Str::limit($package->description,82) }}</p><ul>@foreach(array_slice($package->services ?? [],0,3) as $serviceName)<li><i class="fa-solid fa-check"></i> {{ $serviceName }}</li>@endforeach</ul><div class="package-footer"><div class="price-row"><small>Package price</small><span>&#8377;{{ number_format($package->effective_price) }}</span>@if($package->discount_price)<del>&#8377;{{ number_format((float)$package->price) }}</del>@endif</div><div class="service-card-actions"><form method="post" action="{{ route('cart.add') }}">@csrf<input type="hidden" name="package_id" value="{{ $package->id }}"><input type="hidden" name="city_id" value="{{ $selectedCity?->id }}"><button class="icon-link" title="Add package to cart"><i class="fa-solid fa-cart-plus"></i></button></form><a class="btn btn-dark btn-sm" href="{{ route('booking.create',['package'=>$package->slug]) }}">Book now</a></div></div></div></article>
