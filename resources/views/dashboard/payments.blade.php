@extends('layouts.app')
@section('content')<section class="dashboard-shell"><div class="container"><div class="dashboard-welcome"><div><span class="mini-label">Customer dashboard</span><h1>Payment history</h1><p class="section-lead">Razorpay transactions and booking references.</p></div><a class="btn btn-outline-party" href="{{ route('dashboard') }}">Back to dashboard</a></div><div class="dashboard-panel"><div class="table-responsive"><table class="table align-middle"><thead><tr><th>Date</th><th>Booking</th><th>Transaction</th><th>Method</th><th>Status</th><th>Amount</th></tr></thead><tbody>@forelse($payments as $payment)<tr><td>{{ $payment->created_at->format('d M Y') }}</td><td><a class="fw-bold" href="{{ route('dashboard.booking',$payment->booking) }}">{{ $payment->booking->booking_no }}</a></td><td>{{ $payment->payment_id ?: $payment->razorpay_order_id ?: '-' }}</td><td>{{ ucfirst($payment->method ?: 'Razorpay') }}</td><td><span class="status-badge">{{ $payment->payment_status }}</span></td><td>&#8377;{{ number_format((float)$payment->amount) }}</td></tr>@empty<tr><td colspan="6" class="text-center py-5">No payment records yet.</td></tr>@endforelse</tbody></table></div>{{ $payments->links() }}</div></div></section>@endsection
